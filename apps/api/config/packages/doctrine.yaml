doctrine:
    dbal:
        # Custom DBAL types live next to the bounded context that owns them.
        # `ltree` is a Postgres-only type used by `objects.path` for the
        # category tree (#33 / 0.3.3). Doctrine ORM 3 ships no native
        # mapping; the adapter is a thin pass-through over text.
        types:
            ltree: App\Catalog\Infrastructure\Doctrine\Type\LtreeType

        # Tell Doctrine's schema introspector that the Postgres `ltree`
        # column type maps back to our `ltree` Doctrine type. Without
        # this, `doctrine:schema:drop --full-database` (Foundry's
        # ResetDatabase trait) trips with "Unknown database type ltree".
        mapping_types:
            ltree: ltree

        url: '%env(resolve:DATABASE_URL)%'

        # IMPORTANT: You MUST configure your server version,
        # either here or in the DATABASE_URL env var (see .env file)
        #server_version: '16'

        profiling_collect_backtrace: '%kernel.debug%'
        use_savepoints: true

        # Disable query logging globally — sekcja 3.10 architektury and ticket
        # 0.0.13 call this out as a memory-leak source under FrankenPHP worker
        # mode: the logger keeps every executed query in memory for the lifetime
        # of the worker. Symfony's debug profiler still captures queries through
        # a separate collector, so dev/test debugging is unaffected.
        logging: false

    orm:
        auto_generate_proxy_classes: true
        enable_lazy_ghost_objects: true
        report_fields_where_declared: true
        validate_xml_mapping: true
        naming_strategy: doctrine.orm.naming_strategy.underscore_number_aware
        identity_generation_preferences:
            Doctrine\DBAL\Platforms\PostgreSQLPlatform: identity
        # Each bounded context maps explicitly. auto_mapping would scan
        # the default src/Entity (Symfony skeleton placeholder) and clash
        # with the bundle-style structure under src/{Context}/Domain/Entity.
        auto_mapping: false
        mappings:
            Catalog:
                type: attribute
                is_bundle: false
                dir: '%kernel.project_dir%/src/Catalog/Domain/Entity'
                prefix: 'App\Catalog\Domain\Entity'
                alias: Catalog
            Channel:
                type: attribute
                is_bundle: false
                dir: '%kernel.project_dir%/src/Channel/Domain/Entity'
                prefix: 'App\Channel\Domain\Entity'
                alias: Channel
            Asset:
                type: attribute
                is_bundle: false
                dir: '%kernel.project_dir%/src/Asset/Domain/Entity'
                prefix: 'App\Asset\Domain\Entity'
                alias: Asset
            Identity:
                type: attribute
                is_bundle: false
                dir: '%kernel.project_dir%/src/Identity/Domain/Entity'
                prefix: 'App\Identity\Domain\Entity'
                alias: Identity
        controller_resolver:
            auto_mapping: false

        # Registered (declared) but disabled by default. Each request enables
        # it via TenantFilterConfigurator once the active tenant is known.
        # Native SQL bypasses this filter — RLS in phase 1 closes the gap.
        filters:
            tenant:
                class: App\Identity\Infrastructure\Doctrine\Filter\TenantFilter
                enabled: false

when@test:
    doctrine:
        dbal:
            # "TEST_TOKEN" is typically set by ParaTest
            dbname_suffix: '_test%env(default::TEST_TOKEN)%'

        # Enable query logging in tests — visibility into the SQL the filter
        # produces is worth the small allocation overhead.
        # Re-enable per-test if needed.

when@prod:
    doctrine:
        orm:
            auto_generate_proxy_classes: false
            proxy_dir: '%kernel.build_dir%/doctrine/orm/Proxies'
            query_cache_driver:
                type: pool
                pool: doctrine.system_cache_pool
            result_cache_driver:
                type: pool
                pool: doctrine.result_cache_pool

    framework:
        cache:
            pools:
                doctrine.result_cache_pool:
                    adapter: cache.app
                doctrine.system_cache_pool:
                    adapter: cache.system
