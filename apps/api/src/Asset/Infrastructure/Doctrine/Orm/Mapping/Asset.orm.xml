<?xml version="1.0" encoding="UTF-8"?>
<doctrine-mapping xmlns="http://doctrine-project.org/schemas/orm/doctrine-mapping"
                  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                  xsi:schemaLocation="http://doctrine-project.org/schemas/orm/doctrine-mapping
                                      https://www.doctrine-project.org/schemas/orm/doctrine-mapping.xsd">

    <entity name="App\Asset\Domain\Entity\Asset"
            table="assets"
            repository-class="App\Asset\Infrastructure\Doctrine\Repository\DoctrineAssetRepository">

        <unique-constraints>
            <unique-constraint name="assets_tenant_code_uniq" columns="tenant_id,code"/>
            <unique-constraint name="assets_object_id_uniq" columns="object_id"/>
            <!-- Partial UNIQUE on (tenant_id, content_hash) is created in
                 the migration with `WHERE content_hash IS NOT NULL` — Doctrine
                 ORM XML cannot express partial indexes so we mirror the
                 plain composite for ORM-side reference; the migration is
                 authoritative on the actual schema. -->
        </unique-constraints>

        <indexes>
            <index name="assets_tenant_idx" columns="tenant_id"/>
            <index name="assets_tenant_mime_idx" columns="tenant_id,mime_type"/>
            <index name="assets_tenant_created_idx" columns="tenant_id,created_at"/>
        </indexes>

        <id name="id" type="uuid" column="id">
            <generator strategy="NONE"/>
        </id>

        <many-to-one field="tenant" target-entity="App\Shared\Domain\Tenant">
            <join-column name="tenant_id" referenced-column-name="id" nullable="false" on-delete="RESTRICT"/>
        </many-to-one>

        <field name="code" type="string" length="128"/>

        <field name="objectId" type="uuid" column="object_id" nullable="true"/>

        <field name="originalFilename" type="string" length="255" column="original_filename"/>
        <field name="mimeType" type="string" length="128" column="mime_type"/>
        <field name="size" type="bigint"/>

        <field name="contentHash" type="string" length="64" column="content_hash" nullable="true"/>
        <field name="width" type="integer" nullable="true"/>
        <field name="height" type="integer" nullable="true"/>
        <field name="pageCount" type="integer" column="page_count" nullable="true"/>

        <field name="tags" type="json" column="tags">
            <options>
                <option name="jsonb">true</option>
                <option name="default">[]</option>
            </options>
        </field>

        <field name="thumbnailsStatus" type="string" length="16" column="thumbnails_status">
            <options>
                <option name="default">pending</option>
            </options>
        </field>

        <field name="metadata" type="json">
            <options>
                <option name="jsonb">true</option>
                <option name="default">{}</option>
            </options>
        </field>
        <field name="storagePath" type="string" length="1024" column="storage_path"/>
        <field name="createdAt" type="datetime_immutable" column="created_at"/>

        <one-to-many field="variants" target-entity="App\Asset\Domain\Entity\AssetVariant" mapped-by="asset" orphan-removal="true">
            <cascade>
                <cascade-persist/>
            </cascade>
        </one-to-many>

    </entity>

</doctrine-mapping>
