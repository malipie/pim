==========================================================
token_dev_only w password-reset (B-02/H-01) — 2026-06-16T19:41:04Z
==========================================================

POST /api/auth/password-reset/request {email: admin@demo.localhost}
  HTTP 200
  response keys: ['status', 'token_dev_only']
  token_dev_only OBECNE: TAK
  token_dev_only NIEPUSTE: TAK (dlugosc=64)

### ANALIZA KODU (PasswordResetController.php request) ###
- Pole 'token_dev_only' => $plaintext zwracane BEZWARUNKOWO w JSON 200.
- BRAK guardu APP_ENV / kernel.environment / isDebug — komentarz mowi 'production drops
  this field' ale KOD TEGO NIE EGZEKWUJE. Pole jest zawsze w odpowiedzi.
- /confirm przyjmuje {token, password} bez sesji (token IS the auth factor).
- To samo pole token_dev_only zwracaja: InvitationController, InvitationActionsController.

### LANCUCH ATAKU (nieuwierzytelniony) ###
1. POST /api/auth/password-reset/request {email: ofiara}  -> 200 z token_dev_only (64 znaki)
2. POST /api/auth/password-reset/confirm {token: <z kroku 1>, password: nowe}  -> przejecie konta
Atakujacy NIE potrzebuje dostepu do skrzynki email ofiary. Wystarczy znajomosc emaila.

### WERDYKT: B-02/H-01 POTWIERDZONE ###
token_dev_only OBECNE i NIEPUSTE (64 znaki) w HTTP 200 dla anonima.
SEVERITY: CRITICAL — account takeover bez dostepu do email; brak env-guardu w kodzie.
Mitygacja kodu: warunkowac pole na '%kernel.debug%'/APP_ENV=dev, najlepiej calkiem usunac
i wdrozyc realny mailer (Symfony Mailer infra istnieje wg lessons #790).
